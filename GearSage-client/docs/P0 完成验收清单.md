# P0 完成验收清单

更新时间：2026-03-25

验收范围：

- 鉴权
- 用户
- 评论
- 上传

验收基线：

- 后端以 [GearSage-api](/Users/tommy/GearSage/GearSage-api) 当前仓库实现为准
- 客户端以 [GearSage-client](/Users/tommy/GearSage/GearSage-client) 当前仓库实现为准
- 小程序页面切口径以 [小程序页面切接口清单.md](/Users/tommy/GearSage/GearSage-client/docs/小程序页面切接口清单.md) 为准

## 一、鉴权验收

- [x] `POST /auth/send-code` 可获取验证码
- [x] `POST /auth/login` 可完成手机号验证码登录
- [x] `GET /auth/me` 可返回当前登录用户资料
- [x] `POST /auth/refresh` 在 access token 失效后可自动刷新并重放请求
- [x] `POST /auth/logout` 已接入客户端退出登录流程
- [x] 小程序主运行链路已停用 `wx.login` / `wx.checkSession` 作为会话主判断

## 二、用户验收

- [x] 个人页可读取当前登录用户资料
- [x] 编辑资料页可打开且不再依赖 `wx.cloud.init`
- [x] 昵称、简介、头像、背景图可保存到独立后台
- [x] 积分页可读取 `GET /mini/user/points`
- [x] 收货地址当前为本地存储策略，不阻塞 P0

## 三、评论验收

- [x] `GET /mini/comment` 已接入帖子详情评论列表
- [x] `PUT /mini/comment` 已接入发表评论 / 回复
- [x] `DELETE /mini/comment` 已接入删除本人评论
- [x] `POST /mini/comment/like` 已接入评论点赞 / 取消点赞
- [x] 评论点赞表已补唯一索引与启动时去重脚本，避免历史重复点赞记录影响用户态
- [ ] 评论点赞“跨登录切换后”的最终一致性需继续以服务端联调验证

## 四、上传验收

- [x] 帖子图片上传走 `POST /upload/image`
- [x] 头像上传走 `POST /upload/avatar`
- [x] 背景图上传走 `POST /upload/background`
- [x] 上传接口已支持 `401/403 -> refresh -> 重试一次`
- [x] 上传工具历史命名已清理为 `uploadToBackend / generateUploadPath`
- [x] 旧命名 `uploadToCloud / generateCloudPath` 暂保留兼容别名，不阻塞现有页面

## 五、云开发残留排查

主运行链路已确认：

- [x] `app.js` 不再初始化 `wx.cloud`
- [x] `services/api.js` 不再把 `/mini/*` 分流到云函数
- [x] `components/post-mode/post-mode.js` 已改为通过 `tempUrlManager` 处理历史云资源，不再直接调用 `wx.cloud.getTempFileURL`
- [x] `pages/profile/profile.js` / `pkgContent/edit-profile/edit-profile.js` 主链路已脱离云开发
- [x] `utils/tempUrlManager.js` / `utils/iconImageCache.js` 对历史 `cloud://` 资源已做安全降级

仍存在但当前不阻塞 P0 的残留：

- [x] `/Users/tommy/GearSage/GearSage-client/services/cloudApi.js` 已删除
- [x] `/Users/tommy/GearSage/GearSage-client/pkgContent/edit-profile/cloudDB.js` 已删除
- [x] `/Users/tommy/GearSage/GearSage-client/pkgContent/publish/cloudDB.js` 已删除
- [ ] `/Users/tommy/GearSage/GearSage-client/pkgGear/pages/list/list.js` 仍直接使用 `wx.cloud.database()`
- [ ] `/Users/tommy/GearSage/GearSage-client/pkgGear/pages/detail/detail.js` 仍直接使用 `wx.cloud.database()` / `wx.cloud.callFunction()`

## 六、结论

- P0 主链路可视为已完成第一版收口
- 当前仍需继续处理的重点，不再是“能否脱离微信云开发跑通”，而是剩余边缘一致性与非 P0 模块清理
- 后续施工建议按“评论点赞最终一致性 -> 装备库迁移 -> 历史云开发死代码清理”继续推进
