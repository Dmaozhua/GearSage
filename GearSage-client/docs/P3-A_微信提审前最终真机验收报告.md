# P3-A 微信提审前最终真机验收报告

生成时间：2026-05-12 21:35（Asia/Shanghai）  
验收范围：remote 正式服、短信测试账号口径、微信开发者工具预览、真机 smoke 路径

## 一、测试环境

| 项目 | 结果 |
| --- | --- |
| 小程序 AppID | `wxfcaffa2a2c398e9c` |
| 小程序版本号 | 提审候选版本，尚未上传微信后台生成正式版本号；本次标记为 `P3-A-pre-submit-2026-05-12` |
| 微信开发者工具 | Stable `v2.01.2510280` |
| API 域名 | `https://api.gearsage.club` |
| 静态资源域名 | `https://static.gearsage.club/gearsage` |
| 审核后台地址 | `https://api.gearsage.club/admin-console/` |
| 审核后台账号 | `admin` |
| 审核后台密码 | 不写入仓库文档；提交微信审核时通过微信后台安全字段填写 |

## 二、测试账号与短信口径

| 类型 | 手机号 | 验证码 | 说明 |
| --- | --- | --- | --- |
| 微信审核测试账号 A | `19967820569` | `123456` | remote 白名单固定验证码 |
| 微信审核测试账号 B | `19867820569` | `123456` | remote 白名单固定验证码 |
| 非白名单手机号 | `17700009992` | 不可用 | `POST /auth/send-code` 返回 HTTP 400 |

remote 修复前检查结果：

- `/srv/gearsage-api/.env` 未配置 `SMS_TEST_MODE`、`SMS_TEST_WHITELIST_PHONES`。
- 代码中 `SMS_TEST_MODE` 缺省为 `true`，测试模式下任意 11 位手机号都会写入固定验证码 `123456`。
- 修复前实测 `POST /auth/send-code`，手机号 `17700009991` 返回 `code=123456`、`isTestCode=true`。

本轮处理结果：

- 已补充 `SMS_TEST_WHITELIST_PHONES` 逻辑，仅在测试模式且配置白名单时允许白名单手机号获取固定验证码。
- 未修改 `/auth/send-code`、`/auth/login` 路径。
- 未修改 auth 登录主链路。
- 未修改成功返回 envelope，仍为 `{ code, message, data }`。
- 已在 remote `/srv/gearsage-api/.env` 配置：
  - `SMS_TEST_MODE=true`
  - `SMS_TEST_WHITELIST_PHONES=19967820569,19867820569`
- 已备份 remote env：`.env.bak.sms-whitelist-20260512212513`。
- 已执行 `npm run build` 与 `pm2 restart gearsage-api --update-env`。

补测证据：

| 检查项 | 结果 |
| --- | --- |
| `GET /health` | HTTP 200，`code=0` |
| `GET /health/db` | HTTP 200，数据库 `gearsage`，用户 `gearsage_app` |
| 非白名单 `17700009992` 发码 | HTTP 400，`该手机号暂不支持测试验证码登录` |
| 白名单 `19967820569` 发码 | HTTP 201，`code=123456`，`isTestCode=true` |
| 白名单 `19967820569` 登录 | HTTP 201，登录成功，用户 ID `28` |
| `auth_sms_codes` 留痕 | `phone=19967820569`，`scene=login`，`status=used`，`sendChannel=test_mode` |

登录页展示口径：

- `pages/login/login.js` 发送验证码后只提示“验证码已发送”，不读取或展示 `data.code`。
- `pages/login/login.wxml` 仅展示手机号、验证码输入框和“获取验证码”按钮，不展示固定测试码。
- 提审资料中应把测试手机号与验证码填在微信后台审核资料字段内，不在小程序页面明示测试码。

## 三、微信开发者工具 / 预览

| 步骤 | 结果 |
| --- | --- |
| 手动打开微信开发者工具 | 已打开项目 `GearSage-client` |
| 服务端口 | 已开启，端口 `53033` |
| 重新编译 / 预览 | CLI preview 成功 |
| 预览包体 | TOTAL `3.2 MB`；main `2.0 MB`；`/pkgGear/` `758.9 KB`；`/pkgContent/` `459.2 KB`；`/pkgLabs/` `52.6 KB` |
| 预览二维码 | 已生成 `/Users/tommy/GearSage/.tmp/gearsage-preview-qr.png` |
| remote 环境 | 真机预览默认走 remote；开发者工具内也可在登录页调试区切换到“外网服务器” |

## 四、真机 smoke 路径

本轮已生成微信开发者工具预览二维码，但 Codex 无法代替物理手机扫码和触屏操作。以下真机路径尚未取得人工执行结果：

| 路径 | 当前结果 |
| --- | --- |
| 首页 | 未完成真机确认 |
| 装备库 tab | 未完成真机确认 |
| 装备详情 | 未完成真机确认 |
| 登录 | 后端测试账号口径已通过；真机页面登录未完成确认 |
| 发起求推荐 | 未完成真机确认 |
| 上传图片 | remote 图片审核 provider 已在上一轮通过；真机页面上传未完成确认 |
| 提交待审核 | remote API 已在上一轮确认新帖进入 `status=1`；真机页面未完成确认 |
| 我的发布待审核 | remote API 已在上一轮确认；真机页面未完成确认 |
| 后台通过 | remote API 已在上一轮确认；本轮未新增后台功能 |
| 手机刷新公开可见 | 未完成真机确认 |
| 评论 | remote API 已在上一轮确认；真机页面未完成确认 |
| 举报 | remote API 与入口资料已在上一轮确认；真机页面未完成确认 |
| 我的页协议 / 隐私 / 联系方式 / 注销入口 | 页面入口已在 `docs/小程序页面切接口清单.md` 记录为 P3-A 已补齐；真机页面未完成确认 |

## 五、通过项

- remote 正式服短信测试模式已从“任意手机号固定码”收口为“审核白名单手机号固定码”。
- remote health / db health 正常。
- 白名单测试账号 `19967820569` 可用固定验证码 `123456` 登录。
- 非白名单手机号不能再获取测试验证码。
- 登录页不展示固定测试码。
- 微信开发者工具已打开项目并完成 preview 编译。
- 本轮未修改首页 UI、装备库、审核后台、内容审核逻辑、接口路径或短信接口路径。

## 六、失败项 / 未完成项

| 项目 | 结论 |
| --- | --- |
| 真机扫码全路径验收 | 未完成。已生成预览二维码，但缺少物理手机扫码后的路径执行结果 |
| 是否发现新的功能阻断 | 未发现新的 remote API / SMS 功能阻断 |
| 是否存在提审阻断 | 存在验收阻断：最终真机 smoke 未闭环 |

## 七、微信后台审核说明草稿

GearSage 是面向路亚钓鱼用户的装备资料查询、参数对比与装备经验交流工具。用户可在游客状态浏览首页、装备库和装备详情；发布求推荐、评论、举报等操作需要手机号验证码登录。

审核测试账号：

- 手机号：`19967820569`
- 验证码：`123456`

内容与治理说明：

1. 用户昵称、简介、发帖标题、发帖正文、评论、举报理由接入腾讯云文本审核。
2. 头像、背景图、发帖图片接入腾讯云图片审核。
3. 新发布内容先进入待审核状态，通过审核后台处理后公开展示。
4. 待审核内容不对其他用户公开，且不可评论、不可点赞。
5. 审核记录写入 `moderation_records`，后台处理动作写入 `admin_operation_logs`。
6. 举报入口覆盖内容、评论、用户，举报理由进入文本审核并进入后台处理列表。
7. 审核后台地址：`https://api.gearsage.club/admin-console/`。

建议审核路径：

1. 首页浏览公开内容。
2. 进入装备库 tab，查看装备详情。
3. 使用测试手机号登录。
4. 发起求推荐并上传图片。
5. 确认发布后进入待审核，不对其他用户公开。
6. 使用审核后台通过内容。
7. 手机刷新后确认公开可见。
8. 发布评论并验证举报入口。
9. 在“我的”页查看用户协议、隐私政策、联系方式、账号注销说明。

## 八、最终结论

- 是否可以上传代码：**暂不建议上传为微信审核版本**。当前 remote API、短信测试账号口径、内容审核 provider、图片审核 provider 均已通过补测，但最终真机 smoke 尚未闭环。
- 是否可以提交微信审核：**暂不建议提交**。需先用手机扫码本轮预览二维码，按第四节路径完成真机确认。
- 是否存在阻断项：**存在验收阻断项**，阻断点不是新功能缺陷，而是“微信开发者工具预览已生成，但真机扫码全路径未完成确认”。

解除阻断条件：

用手机扫码预览二维码，并按“首页 -> 装备库 tab -> 装备详情 -> 登录 -> 发起求推荐 -> 上传图片 -> 提交待审核 -> 我的发布待审核 -> 后台通过 -> 手机刷新公开可见 -> 评论 -> 举报 -> 我的页协议/隐私/联系方式/注销入口”完成一次通过记录。通过后即可上传代码并提交微信审核。
