# P1 施工任务单（当前执行版）

更新时间：2026-03-25  
适用对象：Tommy / Codex / Cursor / 后端开发 / 前端联调

---

## 一、P1 当前基线

P0 已确认完成，以下文档为最新基线：

1. `独立后台迁移计划_施工记录版.md`
2. `GearSage_后端架构设计文档.md`
3. `小程序页面切接口清单.md`
4. `P0 完成验收清单.md`

当前进入 P1。

---

## 二、P1 已完成的部分

### 2.1 上传与静态资源链路已打通
已完成：

- `api.gearsage.club` 已上线
- `static.gearsage.club` 已上线
- 后端上传已接入挂载目录
- 实际上传目录拍板为：
  - `/lhcos-data/gearsage`
- 实际资源访问基础路径拍板为：
  - `https://static.gearsage.club/gearsage`
- `POST /upload/image` 已真实验证
- 返回 URL 已可直接访问

### 2.2 当前上传方案（必须知晓）
当前执行方案不是 COS SDK 直连，而是：

> 腾讯云轻量对象存储挂载目录 + Nginx 静态域名映射

必须同步给 Codex 的数据如下：

```text
服务器名称 / ID：lhins-cyli1u6e
挂载根目录：/lhcos-data
业务上传目录：/lhcos-data/gearsage
静态资源域名：https://static.gearsage.club/gearsage
API 域名：https://api.gearsage.club
静态站点 Nginx 配置：/etc/nginx/conf.d/gearsage-static.conf
API 站点 Nginx 配置：/etc/nginx/conf.d/gearsage-api.conf
```

### 2.3 当前已验证示例
```text
上传返回 URL：
https://static.gearsage.club/gearsage/topic/1774422069167-c0w69lbu.txt
```

### 2.4 当前已推进进度（2026-03-25）
已完成：

- 后端 `upload` 模块代码已支持：
  - `UPLOAD_DIR=/lhcos-data/gearsage`
  - `UPLOAD_BASE_URL=https://static.gearsage.club/gearsage`
- `POST /upload/image` 已统一映射到 `topic/` 目录
- `POST /upload/avatar` / `POST /upload/background` 目录口径已固定
- 未配置 `UPLOAD_BASE_URL` 时，仍可回退为本地 `/uploads/*`，保证本地联调不被打断
- `GearSage-api` 的 `README.md`、`.env.example`、`env.example` 已同步到当前 P1 上传方案
- 历史死代码已清理：
  - `services/cloudApi.js`
  - `pkgContent/edit-profile/cloudDB.js`
  - `pkgContent/publish/cloudDB.js`
- `GearSage-api` 已落地标签 / 虚拟权益第一版：
  - `GET /mini/tag/usable`
  - `GET /mini/tag/used`
  - `POST /mini/tag/used`
  - `GET /mini/tag/points`
  - `POST /mini/tag/redeem`
  - `GET /mini/goods?type=0`
  - `POST /mini/goods`
- 前端 `services/api.js` 中标签 / 商城 mock 分流已清理，开发工具将直连真实后台

---

## 三、P1 本轮范围（正式拍板）

本轮 P1 只做以下范围：

1. 收口上传模块
2. 清理非 P0 云开发残留
3. 开始切标签 / 商城 / 任务接口
4. 输出装备库迁移拆分方案

本轮不做：

- 切换为 COS SDK 直连
- 前端直传
- STS 临时密钥
- CDN 架构升级
- 装备库整块迁完
- 管理后台大规模施工

---

## 四、P1 完成标准

本轮 P1 完成标准如下：

1. 上传全部走独立后台
2. 上传全部写入挂载对象存储目录
3. `static.gearsage.club` 可访问上传资源
4. 客户端上传工具命名统一
5. 非 P0 云开发残留完成第一轮清理
6. 标签 / 商城 / 任务链路开始切后台
7. 装备库迁移任务拆分文档输出

---

## 五、P1 优先级排序

## P1-A（立即做）

### A1. 收口上传模块
目标：

- 正式验证 `POST /upload/avatar`
- 正式验证 `POST /upload/background`
- 小程序头像 / 背景图 / 发布页图片做一次全链路复测

验收标准：

- 返回 URL 可访问
- 前端页面能正确显示
- 不再出现旧云开发 `cloud://` 资源写入

当前进度：

- 后端代码口径已完成
- 待继续做真实上传联调验证

### A2. 客户端上传工具收口
目标：

- 清理历史命名
- 确认统一口径只保留独立后台上传路径

重点文件：

- `GearSage-client/utils/imageUploadUtils.js`
- 相关页面上传调用封装

验收标准：

- 主命名统一为后台语义
- 不再误导为“云上传”
- 兼容别名如仍保留，需明确标记为临时兼容

当前进度：

- 已完成
- 当前主命名已统一为：
  - `uploadToBackend`
  - `generateUploadPath`
- 旧命名仅保留兼容别名

### A3. 文档回写
目标：

- 把本次上传与静态资源链路的真实结果回写到施工记录、后端架构、P1 任务单
- 确保 Codex 读取到的口径唯一且最新

---

## P1-B（紧接着做）

### B1. 清理非 P0 云开发残留
当前明确要清理的残留：

- `pkgGear/pages/list/list.js`
- `pkgGear/pages/detail/detail.js`

要求：

- 先扫描哪些仍被引用
- 先清理不再使用的残留
- 对装备库页面残留做单独迁移方案，不在这一轮强行清掉全部

当前进度：

- 已完成第一轮死代码清理：
  - `services/cloudApi.js`
  - `pkgContent/edit-profile/cloudDB.js`
  - `pkgContent/publish/cloudDB.js`
- 当前主要剩余云开发残留已收敛到：
  - `pkgGear/pages/list/list.js`
  - `pkgGear/pages/detail/detail.js`

### B2. 开始切标签 / 商城 / 任务接口
当前建议顺序：

1. 标签
2. 商城
3. 任务

原因：

- 这三块复杂度低于装备库
- 更适合在 P1 中形成新进度
- 能继续扩大独立后台接管范围

当前进度：

- 标签第一版已切到 `GearSage-api`
- 虚拟权益商城（`type=0`）第一版已切到 `GearSage-api`
- 任务接口当前仍为占位实现，待下一轮继续做真实业务逻辑

---

## P1-C（本轮最后做）

### C1. 输出装备库迁移拆分方案
要求：

- 不要求本轮实现整个装备库迁移
- 必须输出：
  - 页面依赖列表
  - 云开发残留点
  - 建议后端接口清单
  - 数据库迁移建议
  - 前后端切换顺序

当前进度：

- 已完成第一版拆分文档：
  - `docs/装备库迁移拆分方案.md`

### C2. 评估历史 `cloud://` 资源清退
要求：

- 统计是否仍有页面依赖历史 `cloud://` URL
- 评估需要保留多久兼容逻辑
- 给出后续清退顺序建议

---

## 六、Codex 当前必须知道的事实

以下内容必须原样同步给 Codex：

1. P0 已完成
2. 当前 P1 上传链路已打通
3. 当前上传不是 COS SDK 直连，而是轻量对象存储挂载目录方案
4. 当前业务上传目录是：
   - `/lhcos-data/gearsage`
5. 当前静态资源基础 URL 是：
   - `https://static.gearsage.club/gearsage`
6. 旧错误路径：
   - `/lhcos-data/data/gearsage`
   禁止继续使用
7. 当前 P1 先不动“更底层云存储接入方式”，而是优先做业务收口和云开发残留清理

---

## 七、推荐直接发给 Codex 的任务描述

```text
P0 已确认完成，以下文档为最新基线：
1. 独立后台迁移计划_施工记录版.md
2. GearSage_后端架构设计文档.md
3. 小程序页面切接口清单.md
4. P0 完成验收清单.md

当前进入 P1。

先同步真实上传方案：
- 当前上传不是 COS SDK 直连
- 当前采用腾讯云轻量对象存储挂载目录方案
- 挂载根目录：/lhcos-data
- 业务上传目录：/lhcos-data/gearsage
- 静态资源域名：https://static.gearsage.club/gearsage
- 静态资源当前经 Nginx A 路线映射
- 旧路径 /lhcos-data/data/gearsage 为错误路径，禁止继续使用

P1 本轮范围拍板如下：
1. 收口 upload 模块，补齐 /upload/avatar 和 /upload/background 的联调验证
2. 清理客户端上传工具历史命名
3. 清理非 P0 云开发残留：
   - services/cloudApi.js
   - pkgContent/edit-profile/cloudDB.js
   - pkgContent/publish/cloudDB.js
   - pkgGear/pages/list/list.js
   - pkgGear/pages/detail/detail.js
4. 开始切标签 / 商城 / 任务相关接口
5. 输出装备库迁移拆分方案
6. 回写施工记录文档中的 P1 进度

P1 完成标准：
- 上传全部走独立后台
- 上传全部落到挂载对象存储目录
- static.gearsage.club 可访问图片
- 非 P0 云开发残留完成第一轮清理
- 标签/商城/任务开始切独立后台
- 输出装备库迁移任务拆分与接口契约建议
```

---

## 八、当前结论

当前 P1 已经不是“是否能开始”，而是**已经开始并拿下了最难的上传链路**。

所以接下来最合理的动作不是继续折腾底层云存储，而是：

1. 收口上传
2. 清理残留
3. 推进标签 / 商城 / 任务
4. 输出装备库迁移方案

这就是当前 P1 的正式执行计划。
