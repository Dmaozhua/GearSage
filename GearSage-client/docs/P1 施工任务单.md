# P1 施工任务单（已完成归档版）

更新时间：2026-03-25  
适用对象：Tommy / Codex / Cursor / 后端开发 / 前端联调

---

## 一、P1 当前基线

P0 已确认完成，以下文档为最新基线：

1. `独立后台迁移计划_施工记录版.md`
2. `GearSage_后端架构设计文档.md`
3. `小程序页面切接口清单.md`
4. `P0 完成验收清单.md`
5. `P1 完成验收清单.md`

当前状态：

- P1 第一版已确认完成
- 本文档转为归档基线
- 后续进入下一阶段时，应新建新的执行任务单，不再继续把本文档当“进行中”使用

---

## 二、P1 已完成的部分

### 2.1 上传与静态资源链路已打通
已完成：

- `api.gearsage.club` 已上线
- `static.gearsage.club` 已上线
- 后端上传已接入挂载目录
- 实际上传目录拍板为：
  - `/lhcos-data/gearsage`
- 实际资源访问基础路径拍板为：
  - `https://static.gearsage.club/gearsage`
- `POST /upload/image` 已真实验证
- 返回 URL 已可直接访问

### 2.2 当前上传方案（必须知晓）
当前执行方案不是 COS SDK 直连，而是：

> 腾讯云轻量对象存储挂载目录 + Nginx 静态域名映射

必须同步给 Codex 的数据如下：

```text
服务器名称 / ID：lhins-cyli1u6e
挂载根目录：/lhcos-data
业务上传目录：/lhcos-data/gearsage
静态资源域名：https://static.gearsage.club/gearsage
API 域名：https://api.gearsage.club
静态站点 Nginx 配置：/etc/nginx/conf.d/gearsage-static.conf
API 站点 Nginx 配置：/etc/nginx/conf.d/gearsage-api.conf
```

### 2.3 当前已验证示例
```text
上传返回 URL：
https://static.gearsage.club/gearsage/topic/1774422069167-c0w69lbu.txt
```

### 2.4 当前已推进进度（2026-03-25）
已完成：

- 后端 `upload` 模块代码已支持：
  - `UPLOAD_DIR=/lhcos-data/gearsage`
  - `UPLOAD_BASE_URL=https://static.gearsage.club/gearsage`
- `POST /upload/image` 已统一映射到 `topic/` 目录
- `POST /upload/avatar` / `POST /upload/background` 目录口径已固定
- 未配置 `UPLOAD_BASE_URL` 时，仍可回退为本地 `/uploads/*`，保证本地联调不被打断
- `GearSage-api` 的 `README.md`、`.env.example`、`env.example` 已同步到当前 P1 上传方案
- 历史死代码已清理：
  - `services/cloudApi.js`
  - `pkgContent/edit-profile/cloudDB.js`
  - `pkgContent/publish/cloudDB.js`
- `GearSage-api` 已落地标签 / 虚拟权益第一版：
  - `GET /mini/tag/usable`
  - `GET /mini/tag/used`
  - `POST /mini/tag/used`
  - `GET /mini/tag/points`
  - `POST /mini/tag/redeem`
  - `GET /mini/goods?type=0`
  - `POST /mini/goods`
- `GearSage-api` 已落地任务第一版：
  - `GET /mini/taskFeat`
  - `GET /mini/taskFeat/unfinish`
  - `POST /mini/taskFeat`
- 前端 `services/api.js` 中标签 / 商城 mock 分流已清理，开发工具将直连真实后台
- 积分商城重复兑换等业务错误提示已优化为前端可读提示

---

## 三、P1 本轮范围（正式拍板）

本轮 P1 只做以下范围：

1. 收口上传模块
2. 清理非 P0 云开发残留
3. 开始切标签 / 商城 / 任务接口
4. 输出装备库迁移拆分方案

本轮不做：

- 切换为 COS SDK 直连
- 前端直传
- STS 临时密钥
- CDN 架构升级
- 装备库整块迁完
- 管理后台大规模施工

---

## 四、P1 完成标准

本轮 P1 完成标准如下，当前均已满足：

1. 上传全部走独立后台
2. 上传全部写入挂载对象存储目录
3. `static.gearsage.club` 可访问上传资源
4. 客户端上传工具命名统一
5. 非 P0 云开发残留完成第一轮清理
6. 标签 / 商城 / 任务链路开始切后台
7. 装备库迁移任务拆分文档输出

---

## 五、P1 实际完成情况

## P1-A（已完成）

### A1. 收口上传模块
目标：

- 正式验证 `POST /upload/avatar`
- 正式验证 `POST /upload/background`
- 小程序头像 / 背景图 / 发布页图片做一次全链路复测

验收标准：

- 返回 URL 可访问
- 前端页面能正确显示
- 不再出现旧云开发 `cloud://` 资源写入

当前进度：

- 已完成
- 头像 / 背景图 / 发布页图片均已完成独立后台链路联调
- 上传路径与静态资源口径已固定为当前正式方案

### A2. 客户端上传工具收口
目标：

- 清理历史命名
- 确认统一口径只保留独立后台上传路径

重点文件：

- `GearSage-client/utils/imageUploadUtils.js`
- 相关页面上传调用封装

验收标准：

- 主命名统一为后台语义
- 不再误导为“云上传”
- 兼容别名如仍保留，需明确标记为临时兼容

当前进度：

- 已完成
- 当前主命名已统一为：
  - `uploadToBackend`
  - `generateUploadPath`
- 旧命名仅保留兼容别名

### A3. 文档回写
目标：

- 把本次上传与静态资源链路的真实结果回写到施工记录、后端架构、P1 任务单
- 确保 Codex 读取到的口径唯一且最新

---

## P1-B（已完成）

### B1. 清理非 P0 云开发残留
当前明确要清理的残留：

- `pkgGear/pages/list/list.js`
- `pkgGear/pages/detail/detail.js`

要求：

- 先扫描哪些仍被引用
- 先清理不再使用的残留
- 对装备库页面残留做单独迁移方案，不在这一轮强行清掉全部

当前进度：

- 已完成第一轮死代码清理：
  - `services/cloudApi.js`
  - `pkgContent/edit-profile/cloudDB.js`
  - `pkgContent/publish/cloudDB.js`
- `pkgGear/pages/list/list.js` 已切到 `GET /mini/gear/brands` / `GET /mini/gear/list`
- `pkgGear/pages/detail/detail.js` 已切到 `GET /mini/gear/detail` / `GET /mini/topic/all`
- 当前主链路剩余云开发残留已不再集中在 `pkgGear` 页面，而是转移到：
  - 装备库导入脚本
  - Excel 源数据中的历史 `cloud://` 路径

### B2. 开始切标签 / 商城 / 任务接口
当前建议顺序：

1. 标签
2. 商城
3. 任务

原因：

- 这三块复杂度低于装备库
- 更适合在 P1 中形成新进度
- 能继续扩大独立后台接管范围

当前进度：

- 标签第一版已切到 `GearSage-api`
- 虚拟权益商城（`type=0`）第一版已切到 `GearSage-api`
- 任务第一版已切到 `GearSage-api`
- 当前任务第一版已支持：
  - 每日登录 / 发帖 / 评论 / 点赞
  - 完善资料
  - 邀请好友
  - 发帖达人 / 评论达人 / 点赞达人
- 后续再继续补自动记录、更多任务种类和后台配置能力
- 装备库主数据链已进入第一版正式落库：
  - gear 表结构已补到 PostgreSQL
  - `npm run import:gear` 已可把 `rate/excel` 导入库中
  - `GearService` 当前已读库优先、Excel 回退

---

## P1-C（已完成）

### C1. 输出装备库迁移拆分方案
要求：

- 不要求本轮实现整个装备库迁移
- 必须输出：
  - 页面依赖列表
  - 云开发残留点
  - 建议后端接口清单
  - 数据库迁移建议
  - 前后端切换顺序

当前进度：

- 已完成第一版拆分文档：
  - `docs/装备库迁移拆分方案.md`
- 已完成装备库第一版接口与页面切换：
  - `GET /mini/gear/brands`
  - `GET /mini/gear/list`
  - `GET /mini/gear/detail`
  - `pkgGear/pages/list/list.js`
  - `pkgGear/pages/detail/detail.js`
- `GET /mini/topic/all` 已补装备关联筛选，装备详情页“相关推荐”已切到独立后台
- 已完成装备库第一版正式数据链：
  - `gear_brands / gear_master / gear_variants`
  - `scripts/import_gear_excel.js`
  - gear 接口当前已优先读取 PostgreSQL

### C2. 评估历史 `cloud://` 资源清退
要求：

- 统计是否仍有页面依赖历史 `cloud://` URL
- 评估需要保留多久兼容逻辑
- 给出后续清退顺序建议

---

## 六、Codex 当前必须知道的事实

以下内容必须原样同步给 Codex：

1. P0 已完成
2. P1 第一版已完成
3. 当前上传不是 COS SDK 直连，而是轻量对象存储挂载目录方案
4. 当前业务上传目录是：
   - `/lhcos-data/gearsage`
5. 当前静态资源基础 URL 是：
   - `https://static.gearsage.club/gearsage`
6. 旧错误路径：
   - `/lhcos-data/data/gearsage`
   禁止继续使用
7. 当前 P1 已完成收口，后续新阶段不应回退既有方案

---

## 七、P1 归档结论

- 上传与静态资源链路：已完成
- 非 P0 云开发残留第一轮清理：已完成
- 标签 / 商城 / 任务第一版切后台：已完成
- 装备库列表 / 详情 / 相关推荐第一版切后台：已完成
- 装备库 `Excel -> PostgreSQL` 导入链第一版：已完成

当前建议：

1. 本文档停止继续追加“进行中”任务
2. 下一阶段新建新的执行任务单
3. 当前真实施工状态继续以 `独立后台迁移计划_施工记录版.md` 为准
