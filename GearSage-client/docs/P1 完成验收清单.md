# P1 完成验收清单

更新时间：2026-03-25

## 一、验收结论

P1 第一版已完成，可作为下一阶段施工基线。

## 二、已验收能力

1. 上传链路已全部切到独立后台：
   - `POST /upload`
   - `POST /upload/image`
   - `POST /upload/avatar`
   - `POST /upload/background`
2. 上传资源当前统一落到：
   - `/lhcos-data/gearsage`
3. 静态资源访问口径已固定：
   - `https://static.gearsage.club/gearsage`
4. 客户端上传工具主命名已统一为后台语义。
5. 标签第一版已切到独立后台：
   - `usable / used / points / redeem`
6. 虚拟权益商城第一版已切到独立后台：
   - `GET /mini/goods?type=0`
   - `POST /mini/goods`
7. 任务第一版已切到独立后台：
   - `GET /mini/taskFeat`
   - `GET /mini/taskFeat/unfinish`
   - `POST /mini/taskFeat`
8. 装备库第一版页面切换已完成：
   - 装备列表
   - 装备详情
   - 相关推荐
9. 装备库数据链第一版已完成：
   - `rate/excel -> PostgreSQL`
   - `npm run import:gear`
   - 同步重建 `pkgGear/searchData/Data.js`

## 三、当前正式口径

1. 当前真实施工状态以 `独立后台迁移计划_施工记录版.md` 为准。
2. 长期设计以 `GearSage_后端架构设计文档.md` 为准。
3. 页面接口现状以 `小程序页面切接口清单.md` 为准。
4. P1 已完成，后续新阶段应另建新的执行任务单。

## 四、P1 遗留但不阻塞收口的项

1. 评论点赞用户态一致性仍建议继续做更深入联调确认。
2. 历史 `cloud://` 资源与旧资产清退仍有后续空间。
3. 任务模块当前仍是最小可用第一版。
4. 装备库搜索 / 推荐 / 更完整筛选字段仍需继续打磨。
