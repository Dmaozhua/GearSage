# 小程序页面切接口清单

更新时间：2026-04-01

基准说明：

- 本清单以 [GearSage-api](/Users/tommy/GearSage/GearSage-api) 当前仓库实现为唯一口径
- 当前已覆盖 P0 全量 + P1 第一版：标签 / 虚拟权益商城 / 任务 / 装备库第一版
- “页面”一列按微信小程序实际页面或直接承载页面调用的工具层组织

## 一、P0 接口基线

### 1. 鉴权

- `POST /auth/send-code`
- `POST /auth/login`
- `POST /auth/refresh`
- `POST /auth/logout`
- `GET /auth/me`

### 2. 用户

- `GET /mini/user/info?id=:id`
- `POST /mini/user/update`
- `GET /mini/user/points`

### 3. 评论

- `GET /mini/comment?topicId=:id`
- `PUT /mini/comment`
- `POST /mini/comment/like`
- `DELETE /mini/comment?commentId=:id`
- `POST /mini/topic/recommend/accept`
- `POST /mini/topic/recommend/feedback`

### 4. 上传

- `POST /upload`
- `POST /upload/image`
- `POST /upload/avatar`
- `POST /upload/background`

### 5. 标签 / 虚拟权益 / 任务 / 装备库（P1 已完成第一版）

- `GET /mini/tag/usable`
- `GET /mini/tag/used`
- `POST /mini/tag/used`
- `GET /mini/tag/points`
- `POST /mini/tag/redeem`
- `GET /mini/goods?type=0`
- `POST /mini/goods`
- `GET /mini/taskFeat`
- `GET /mini/taskFeat/unfinish`
- `POST /mini/taskFeat`
- `GET /mini/gear/brands?type=reels|rods|lures`
- `GET /mini/gear/list`
- `GET /mini/gear/detail`
- `GET /mini/user/gear`
- `POST /mini/user/gear`
- `PUT /mini/user/gear/:id`
- `DELETE /mini/user/gear/:id`
- `POST /mini/recommend/selection`

## 二、页面切换清单

| 页面/入口 | 文件 | 当前调用接口 | 状态 | 备注 |
| --- | --- | --- | --- | --- |
| 登录页 | `/Users/tommy/GearSage/GearSage-client/pages/login/login.js` | `POST /auth/send-code` `POST /auth/login` `GET /auth/me` | 已切换 | 登录成功后会立即刷新一次当前用户资料 |
| 全局鉴权运行时 | `/Users/tommy/GearSage/GearSage-client/services/auth.js` | `POST /auth/refresh` `POST /auth/logout` `GET /auth/me` | 已切换 | `access token / refresh token` 为主，已脱离 `wx.login` 主链路 |
| 全局请求层 | `/Users/tommy/GearSage/GearSage-client/services/api.js` | 统一鉴权重试、上传重试、P0 业务封装 | 已切换 | `401/403` 会自动尝试 refresh；上传接口也已支持重试 |
| 首页 | `/Users/tommy/GearSage/GearSage-client/pages/index/index.js` | `GET /mini/gear/list` `GET /mini/topic/all` | P3-A 已收口 | 2026-05-09 首页从大面积帖子流收口为“装备资料工具首页 + 装备经验预览”；首屏展示品牌定位、装备搜索入口、Hero 装备库入口、装备库/装备对比/求推荐/装备经验四入口，以及最近更新装备/最新求推荐/装备经验精选三个传统列表预览模块；不新增 API，不展示积分商城和“更多”入口 |
| 装备库 tab 首页 | `/Users/tommy/GearSage/GearSage-client/pages/gear-tab/index.js` | `GET /mini/gear/list` `POST /mini/recommend/selection` | P3-A 已新增 / P3-B 已补选型入口 | 新增主包 tab 页面 `pages/gear-tab/index`，底部 tab 从“积分”调整为“装备库”；该页本身展示装备分类、分类内搜索入口、装备对比入口、帮我选装备入口、最近更新装备，不做 `onShow` 自动跳分包；分类、详情、对比、选型动作再进入 `pkgGear` 分包页面 |
| 个人页 | `/Users/tommy/GearSage/GearSage-client/pages/profile/profile.js` | `GET /auth/me` `POST /auth/logout` `GET /mini/user/gear` | 已切换 / 我的装备已新增 | 资料展示走当前登录用户；退出登录已改为服务端登出 + 本地清理；当前会读取“我的装备”摘要并展示入口 |
| 我的装备页 | `/Users/tommy/GearSage/GearSage-client/pkgContent/my-gear/my-gear.js` | `GET /mini/user/gear` `PUT /mini/user/gear/:id` `DELETE /mini/user/gear/:id` | 已新增 / 本地验收通过 | 按鱼轮 / 鱼竿 / 常用饵管理当前用户装备；自己可见公开和私密装备，可切换公开状态与删除；2026-05-14 本地 API smoke 与微信开发者工具 preview 编译通过 |
| 我的装备编辑页 | `/Users/tommy/GearSage/GearSage-client/pkgContent/my-gear-edit/my-gear-edit.js` | `GET /mini/gear/list` `GET /mini/gear/detail` `GET /mini/user/gear` `POST /mini/user/gear` `PUT /mini/user/gear/:id` | 已新增 / 本地验收通过 | 支持从装备库搜索添加、从装备详情预填添加、编辑使用状态 / 公开状态 / 备注；2026-05-14 已验证重复添加拦截、软删除后重加、私密过滤与归属权限 |
| 编辑资料页 | `/Users/tommy/GearSage/GearSage-client/pkgContent/edit-profile/edit-profile.js` | `GET /auth/me` `POST /mini/user/update` `POST /upload/avatar` `POST /upload/background` | 已切换 | 页面已脱离云开发；头像/背景走独立后台上传 |
| 帖子详情评论区 | `/Users/tommy/GearSage/GearSage-client/pkgContent/detail/detail.js` | `GET /mini/comment` `PUT /mini/comment` `POST /mini/comment/like` `DELETE /mini/comment` `POST /mini/topic/recommend/accept` `POST /mini/topic/recommend/feedback` `GET /mini/user/info?id=:id` | 已切换 | 评论发布/删除/点赞第一版已接入；`讨论&提问 -> 求推荐` 已新增“写规范推荐 / 补充评论 / 补充反馈”链路，`commentType=recommend_answer` 与 `recommendAnswerMeta` 已接入同一评论主链路；楼主现可采纳 1 条规范回答并补“结果反馈”，详情页会展示独立反馈卡并把帖子状态推进到 `已反馈购买结果`；若帖子开启 `quickReplyOnly`，规范回答入口会默认进入简版回答，但仍可切到完整模板；作者头像/昵称现支持进入他人主页，规范回答卡已补轻量身份沉淀展示，并支持 `回答 X / 被采纳 Y / 获赞 Z` 这类身份线；陈旧详情页异常已补：停留期间帖子被后台下架后，点赞/发表评论遇到 `404 topic not found` 统一提示“帖子已下架”，并关闭当前页点赞/评论/分享入口 |
| 发布页上传 / 求推荐发布 | `/Users/tommy/GearSage/GearSage-client/pkgContent/publish/publish.js` `/Users/tommy/GearSage/GearSage-client/pkgContent/publishMode/publishMode.js` `/Users/tommy/GearSage/GearSage-client/pkgContent/components/post-Question/post-Question.js` | 通过上传工具间接走 `POST /upload/image`；求推荐选择已有装备走 `GET /mini/user/gear` | 已切换 / 我的装备已新增 / P3-B 已兼容选型预填 | 发布主流程图片上传已改走独立后台；当前主发布入口实际为 `publishMode -> post-question` 组件链路，`讨论&提问` 已支持 `recommendMeta` 结构化求推荐字段；求推荐可从“我的装备”选择并写入 `recommendMeta.currentGear/currentGearItems`；P3-B 新增 `from=selection_guide` 本地缓存预填，保留 `selectionSource` 并继续通过 `/mini/topic` 发布 |
| 上传工具 | `/Users/tommy/GearSage/GearSage-client/utils/imageUploadUtils.js` | `POST /upload/image` `POST /upload/avatar` `POST /upload/background` | 已切换 | 主命名已统一为 `uploadToBackend`，旧命名仅保留兼容别名 |
| 积分页 | `/Users/tommy/GearSage/GearSage-client/pages/points/points.js` | `GET /mini/user/points` | 已切换 | 只在 P0 用户积分读取范围内；2026-05-09 已从底部 tab 移入“我的”页快捷入口 |
| 积分兑换页 | `/Users/tommy/GearSage/GearSage-client/pkgContent/points-exchange/points-exchange.js` | `GET /mini/user/points` | 已切换 | 只在 P0 用户积分读取范围内 |
| 我的标签页 | `/Users/tommy/GearSage/GearSage-client/pkgContent/my-tags/my-tags.js` | `GET /mini/tag/used` | P1 已切换 | 标签资料页当前已直连独立后台 |
| 标签管理页 | `/Users/tommy/GearSage/GearSage-client/pkgContent/tag-manager/tag-manager.js` | `GET /mini/tag/usable` `GET /mini/tag/used` `POST /mini/tag/used` | P1 已切换 | 标签佩戴与展示偏好第一版已接入 |
| 积分商城虚拟权益页签 | `/Users/tommy/GearSage/GearSage-client/pkgContent/points-exchange/points-exchange.js` | `GET /mini/goods?type=0` `POST /mini/goods` | P1 已切换 | `services/api.js` 中旧 mock 已清理 |
| 积分页任务区 | `/Users/tommy/GearSage/GearSage-client/pages/points/points.js` | `GET /mini/taskFeat` `GET /mini/taskFeat/unfinish` `POST /mini/taskFeat` | P1 已切换 | 当前为最小可用任务第一版，已支持展示、领取积分 |
| 装备列表页 | `/Users/tommy/GearSage/GearSage-client/pkgGear/pages/list/list.js` | `GET /mini/gear/brands` `GET /mini/gear/list` `POST /mini/recommend/selection` | P1 已切换 / P3-B 已补选型入口 | 已脱离 `wx.cloud.database()`，gear 数据当前由 PostgreSQL 优先返回；当前 list 返回允许增量携带 `official_specs / gsc_traits / compare_profile`；`is_show=0` 的装备不返回，`image_display_status=hidden` 的装备只返回 `/images/empty.png` 占位图；鱼竿列表新增选型向导入口 |
| 选型向导页 | `/Users/tommy/GearSage/GearSage-client/pkgGear/pages/selection-guide/selection-guide.js` | `POST /mini/recommend/selection` | P3-B 已新增 / P3-B-2B 已补 session | 第一版支持鱼竿需求输入，生成 3~6 条选择分支；结果可跳装备详情、写入本地对比池、点击证据帖进入帖子详情、通过 `recommend_prefill_from_selection` 预填求推荐发布页；推荐生成会写入 `gear_selection_sessions` 并返回 `sessionId` |
| 装备详情页 | `/Users/tommy/GearSage/GearSage-client/pkgGear/pages/detail/detail.js` `/Users/tommy/GearSage/GearSage-client/pkgGear/pages/feedback/feedback.js` | `GET /mini/gear/detail` `GET /mini/topic/all` `GET /mini/user/gear` `POST /mini/user/gear` `POST /mini/gear/feedback` | P1 已切换 / P3-A 反馈已补 / 我的装备已新增 | 已脱离 `wx.cloud.database()` 与 `miniApi gear.detail/topic.all`，gear 详情当前由 PostgreSQL 优先返回；现已补“去求推荐”入口，可带当前 gear 预填跳到 `publishMode -> 讨论&提问 -> 求推荐`；当前 detail 返回允许增量携带 `official_specs / gsc_traits / compare_profile`，并为子型号增量暴露 `variantId/sourceKey/gearId` 供资料反馈定位；`is_show=0` 直达返回不可见，`image_display_status=hidden` 使用 `/images/empty.png`；底部新增“发现资料错误 / 补充信息 / 权利问题反馈”入口，提交后进入后台装备反馈页签；详情 CTA 可将当前选中 SKU 加入“我的装备” |
| 装备对比页 | `/Users/tommy/GearSage/GearSage-client/pkgGear/pages/compare/compare.js` | `GET /mini/gear/detail` | P2 增量补充 | 当前不新增 compare 专用接口；页面读取本地对比池后，按 `masterId + variantKey` 复用 gear detail 聚合 2~3 个同类候选，并可跳到 `publishMode -> 讨论&提问 -> 求推荐` |
| 他人主页 | `/Users/tommy/GearSage/GearSage-client/pkgContent/user-profile/user-profile.js` | `GET /mini/user/info?id=:id` `GET /mini/user/gear?userId=:id` | 已切换 / 我的装备已新增 | 第一版已接入基础资料、求推荐沉淀统计、最近被采纳回答与最近帖子摘要；当前主页主统计已覆盖 `求推荐回答 / 被采纳 / 发起求推荐 / 已解决`，并在有值时补充 `回答获赞 / 已补反馈`；登录态下返回 `isReported`，用于进入已举报用户主页时直接显示“已举报”；常用装备模块只展示公开装备且无公开装备时隐藏 |
| 举报入口 | `/Users/tommy/GearSage/GearSage-client/pages/index/index.js` `/Users/tommy/GearSage/GearSage-client/pkgContent/detail/detail.js` `/Users/tommy/GearSage/GearSage-client/pkgContent/user-profile/user-profile.js` | `POST /mini/report` | P3-A 已补齐 | 首页帖子更多菜单、详情页帖子/评论操作区、详情页二级回复、他人主页均可提交举报；详情页对同一用户已举报的帖子/评论显示“已举报”，他人主页对同一用户已举报的用户显示“已举报”，再次点击仅提示“后台正在处理举报信息”，不重复发送举报请求；后端按 `reporterUserId + targetType + targetId` 兜底拦截重复举报；举报理由走文本审核并进入后台举报处理列表 |
| 合规制度页 | `/Users/tommy/GearSage/GearSage-client/pages/legal/index.js` `/Users/tommy/GearSage/GearSage-client/pages/profile/profile.js` `/Users/tommy/GearSage/GearSage-client/pages/login/login.js` `/Users/tommy/GearSage/GearSage-client/pkgContent/publishMode/publishMode.js` `/Users/tommy/GearSage/GearSage-client/pkgContent/postPreview/postPreview.js` | 本地静态文案 | P3-A 已补齐 | 新增主包 `pages/legal/index?type=`，覆盖隐私政策、用户协议、内容发布规范、举报与申诉、账号注销说明、联系我们、关于 GearSage；我的页可见入口、登录页协议链接、发布页内容规范链接均可打开 |

## 三、接口有实现但页面暂未直连的项

| 接口 | 后端状态 | 当前页面使用情况 | 备注 |
| --- | --- | --- | --- |
| `POST /upload` | 已实现 | 目前主流程未直接使用 | 作为兼容旧上传入口保留 |
| `POST /mini/report` | P3-A 已实现 | 首页帖子举报、详情页评论举报、他人主页用户举报已直连 | 保持 `{ code, message, data }`；举报理由纳入 `report_reason` 文本审核 |
| `POST /mini/gear/feedback` | P3-A 已实现 | 装备详情页底部资料反馈入口已直连 | 保持 `{ code, message, data }`；反馈正文纳入 `gear_feedback_content` 文本审核，进入审核后台“装备反馈”页签 |

## 四、P0 联调结论

- 鉴权第一版：已形成 `send-code -> login -> me -> refresh -> logout` 闭环
- 用户第一版：当前用户资料读取、编辑资料保存、积分读取已接到独立后台
- 评论第一版：评论列表、发表评论、删除评论、评论点赞已切到独立后台
- 上传第一版：帖子图片、头像、背景图上传已切到独立后台本地上传目录
- 标签第一版：`my-tags / tag-manager` 已切到独立后台
- 虚拟权益商城第一版：`points-exchange` 的 `type=0` 页签已切到独立后台
- 任务第一版：积分页任务/成就已切到独立后台
- 装备库第一版：`pkgGear` 列表页 / 详情页 / 相关推荐已切到独立后台

## 五、当前遗留

- 评论点赞在“登录切换后再次进入详情页”的用户态一致性已补服务端唯一约束与显式聚合，仍建议继续做一轮真机联调确认
- 任务模块当前仍是最小可用第一版，后续还需要补更完整的任务规则、自动记账与后台配置能力
- 装备库当前已切到 PostgreSQL 优先读取，但维护源仍是 `rate/excel`
- 装备库搜索 / 推荐 / 更细筛选字段仍需继续打磨
